import { DecksManager } from '@/components/DecksManager';

export const dynamic = 'force-dynamic';
export const metadata = {
  title: 'Decks compartilhados — Estudo Simples',
};

export default function DecksPage() {
  return <DecksManager />;
}
