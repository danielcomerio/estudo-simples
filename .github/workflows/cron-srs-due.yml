on:
  schedule:
    - cron: "0 12 * * *"   # 12h UTC = ~9h Brasil
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: |
          curl -X GET https://app.estudosimples.com.br/api/cron/srs-due \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"