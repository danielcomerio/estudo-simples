on:
  schedule:
    - cron: "0 22 * * *"   # 22h UTC = ~19h Brasil
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: |
          curl -X GET https://app.estudosimples.com.br/api/cron/streak-risk \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"